import { rand } from 'dummy/helpers/rand';
import { module, test } from 'qunit';

module('Unit | Helper | rand');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rand([42]);
  assert.ok(result);
});
