import { e } from 'dummy/helpers/e';
import { module, test } from 'qunit';

module('Unit | Helper | e');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = e();
  assert.equal(result, 2.718281828459045, 'The E helper returns a number close enough to the Euler constant as possible');
});
